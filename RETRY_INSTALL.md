**Shayad file create nahi hui thi. Maine ab dobara create kar di hai.**

Please aab run karein:

```powershell
.\install_k8s_tools.ps1
```

Agar phir bhi error aaye, toh verify karne ke liye `ls` ya `dir` command run karein aur dekhein ki `install_k8s_tools.ps1` list ho raha hai ya nahi.
