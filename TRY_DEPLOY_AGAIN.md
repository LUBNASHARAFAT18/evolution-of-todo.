**Maine script `deploy_k8s.ps1` ko update kiya hai taaki woh Minikube ko manually dhoond sake.**

Aap please dobara try karein:

```powershell
.\deploy_k8s.ps1
```

Agar yeh abhi bhi nahi chalta, toh **Computer Restart** karna behtar hoga kyunki PATH variables update hone mein kabhi kabhi Windows restart maangta hai after fresh install.
